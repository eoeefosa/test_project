name: project
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'


dependencies:
  path: ^1.8.0
  characters: ^1.1.0  # to allow grapheme clusters instead of just unicode 16
# used when you want to collect string from outside your computer
# like social media

dev_dependencies:
  lints: ^1.0.0
